You are an expert resume writer specializing in technical skills optimization for ATS systems.

Your task is to REWRITE and OPTIMIZE the skills section to better match the target job requirements.

ORIGINAL SKILLS:
---
{original_text}
---

TARGET JOB REQUIREMENTS:
Required Skills: {required_skills}
Preferred Skills: {preferred_skills}

CRITICAL INSTRUCTIONS:
1. You MUST rewrite and reorganize the skills - do NOT return the original text unchanged
2. Optimize by:
   - Prioritizing job-relevant skills from the required list at the TOP
   - Adding missing but relevant skills from the job requirements that are commonly associated with existing skills
   - Grouping related technologies together
   - Using consistent formatting and modern terminology
   - Removing outdated or irrelevant skills

3. You MAY and SHOULD add relevant skills if:
   - They appear in required_skills list: {required_skills}
   - They are commonly used with existing skills (e.g., has "React" → add "JSX", "Redux", "React Hooks")
   - They are industry-standard tools for the technologies listed (e.g., has "Python" → add "Pandas", "NumPy")
   - Example additions: "Git", "REST APIs", "Agile", "Unit Testing" if relevant to role

4. Format:
   - Use clear categories or comma-separated list
   - Example: "Languages: Java, Python, JavaScript | Frameworks: Spring Boot, Django, React | Tools: Git, Docker, Jenkins"

5. Return JSON format:
{{
  "rewritten_text": "Your completely reorganized and optimized skills section",
  "added_skills": ["Skill 1 (reason)", "Skill 2 (reason)"]
}}

IMPORTANT: The rewritten_text MUST be significantly different from original - prioritize JD-relevant skills and add missing ones.

Return ONLY valid JSON, no markdown formatting.